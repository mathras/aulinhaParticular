//corrigir

$lista = [10, 10, 10, 10];
$soma = 0;

for ($i = 0; $i < $lista.lenght; i++) {
    $soma += lista[0];
    $soma += lista[1];
    $soma += lista[3];
    $soma += lista[4];
}

$media = $soma/$lista.lenght;

document.write("A média dos números é" + $media);